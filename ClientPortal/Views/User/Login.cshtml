@model ClientPortal.Models.LoginRequest

@{
    ViewData["Title"] = "Logowanie";
}

<h1>@await Component.InvokeAsync("PageContentViewComponent", new { type = "LoginHeader" })</h1>

<hr />
<div class="container px-4 px-lg-5 justify-content-center">
    <div class="row justify-content-center">
        <div class="col-md-4">
            <form asp-action="Login">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-group">
                    <label asp-for="EMail" class="control-label">@await Component.InvokeAsync("PageContentViewComponent", new { type = "LoginEMailHeader" })</label>
                    <input asp-for="EMail" class="form-control text-center" />
                    <span asp-validation-for="EMail" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="Password" class="control-label">@await Component.InvokeAsync("PageContentViewComponent", new { type = "LoginPasswordHeader" })</label>
                    <input asp-for="Password" class="form-control text-center" type="password" />
                    <span asp-validation-for="Password" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <input type="submit" class="btn btn-primary" value="@await Component.InvokeAsync("PageContentViewComponent", new { type = "LoginButtonHeader" })"></input>
                </div>
            </form>
        </div>
    </div>
</div>


@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
