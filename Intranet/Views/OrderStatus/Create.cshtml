@model Data.CreateEditVMs.OrderStatusCreateEditVM

@{
    ViewData["Title"] = "Create";
}

<h1>Dodaj nowy status zamówienia</h1>

<hr />
<div class="row">
    <div class="col-md">
        <form method="post" asp-route-id="@ViewBag.Id" asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="StatusName" class="control-label">Status zamówienia</label>
                <input asp-for="StatusName" class="form-control text-center" />
                <span asp-validation-for="StatusName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="IsActive" class="control-label"></label>
                <input asp-for="IsActive" class="form-control text-center" value="True" readonly/>
                <span asp-validation-for="IsActive" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Dodaj" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
            <a asp-action="Index"><button class="btn btn-primary">Powrót</button></a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
